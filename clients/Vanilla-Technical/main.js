import './style.css'

window.pokedex = {
  allPokemon: [],
  favorites: [],
};

const localAPI = 'http://localhost:5050/api/pokemon/';

/**
 * Fetch a list of Pokemon from the specified API endpoint.
 * 
 * To get a specific pokemon, append the pokemon name to the API endpoint.
 */

const init = async () => {
  console.log("Check main.js to start coding!")

}

init()